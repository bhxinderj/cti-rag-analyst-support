% Dokumentationstabelle KI-basierte Hilfsmittel (FHTW-Vorlage).
% Spalten: Tool & Verwendungszweck & Prompt/Quelle/Seite/Absatz.
\aitoolentry{Claude (Anthropic, via Claude Code)}
    {Writing and editing support: drafting of section text under the
     author's direction and revision; proofreading; consistency checks
     of terminology, numbers, and cross-references against the
     evaluation artifacts}
    {Interactive sessions; entire document}
\aitoolentry{Claude (Anthropic, via Claude Code)}
    {Generation of result figures and appendix tables from the
     versioned evaluation artifacts via scripts}
    {Scripts in the prototype repository
     (Appendix~\ref{app:repository}); result figures and the tables of
     Appendices C and D}
\aitoolentry{Claude (Anthropic, via Claude Code)}
    {Code assistance during prototype implementation and evaluation
     tooling}
    {Prototype repository (Appendix~\ref{app:repository})}
\aitoolentry{NotebookLM (Google)}
    {Literature support: locating and cross-checking supporting
     passages in the collected sources}
    {Queries against the source collection of this thesis;
     Chapters 1 and 2}
