\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{csquotes}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{acronym} % geladen von twbook.cls; nohyperlinks via \PassOptionsToPackage in mt_bhinder.tex
\usepackage[most]{tcolorbox}
\usepackage{listings}
\usepackage{float} % erlaubt [H] fuer harte Float-Platzierung
\usepackage{longtable} % seitenumbrechende Tabellen
\usepackage{pdflscape} % echte Querformat-Seiten (PDF-Viewer zeigt horizontal)
% Absatzstil: kein Erstzeilen-Einzug, stattdessen vertikaler Abstand
% zwischen Absaetzen (Blockstil wie in der Salma-Arbeit).
\usepackage{parskip}

% Bibliografie: kein haengender Einzug in den Folgezeilen, dafuer
% kleiner Abstand zwischen den Eintraegen.
\setlength{\bibhang}{0pt}
\setlength{\bibitemsep}{0.6\baselineskip}

% Sentence Case auch fuer die generierten Verzeichnis-Titel
% (Konsistenz mit den Kapitel-/Section-Ueberschriften).
\addto\captionsenglish{%
    \renewcommand*{\listfigurename}{List of figures}%
    \renewcommand*{\listtablename}{List of tables}%
}
\renewcommand*{\listacroname}{List of abbreviations}
\graphicspath{{images/}}

% Stil fuer Prompt-/Code-Listings (z.B. lst:system-prompt in Kap. 3.5)
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    framerule=0.5pt,
    xleftmargin=2mm,
    captionpos=b}

% Stil fuer die vollstaendigen Systemantworten in Anhang E
% (generiert von scripts/appendix_to_tex.py)
\lstdefinestyle{appendixoutput}{
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    frame=single,
    framerule=0.5pt,
    xleftmargin=2mm,
    aboveskip=4pt,
    belowskip=10pt}

% Box fuer Forschungsfragen (Kap. 1) und deren Beantwortung (Kap. 4).
% Schlichter Rahmen: duenne graue Linie, weisser Koerper, hellgraue
% Titelzeile, eckige Kanten.
% Box fuer Antwort-Auszuege in Kap. 4: leicht gruen getoent ---
% Gruen = Output des Prototyps (V4), Blau = RQ-Boxen der Arbeit,
% Grau bleibt fuer technische Listings.
\definecolor{respframe}{HTML}{82B366}
\definecolor{resptitle}{HTML}{D5E8D4}
\definecolor{respback}{HTML}{F6FBF6}
\newtcolorbox{responsebox}[1]{%
    breakable,
    sharp corners,
    colback=respback,
    colframe=respframe,
    colbacktitle=resptitle,
    coltitle=black,
    fonttitle=\bfseries\small,
    fontupper=\small,
    title={#1},
    boxrule=0.5pt,
    left=2mm, right=2mm, top=1.5mm, bottom=1.5mm}

% Leicht blau getoent (Farbfamilie der Pipeline-Diagramme); die
% responsebox bleibt bewusst grau (Artefakt-Output vs. Argumentation).
\definecolor{rqframe}{HTML}{6C8EBF}
\definecolor{rqtitle}{HTML}{DBEAFE}
\definecolor{rqback}{HTML}{F7FAFF}
\newtcolorbox{rqbox}[1]{%
    breakable,
    sharp corners,
    colback=rqback,
    colframe=rqframe,
    colbacktitle=rqtitle,
    coltitle=black,
    fonttitle=\bfseries\small,
    fontupper=\small,
    title={#1},
    boxrule=0.5pt,
    before skip=6pt, after skip=6pt,
    left=2mm, right=2mm, top=1.5mm, bottom=1.5mm}